## 2026-05-22: KI-015 依赖 Import 修复

**What**: 修复 PfFormItem.vue、PfForm.vue、PfDataTableForm.vue 三文件中缺失的子组件 import，以及对应的 registry 依赖链。

**Why**: 
- PfFormItem.vue 使用 `<Input>`, `<pf-icon-picker>`, `<pf-form-item-toggle>`, `<pf-text>`, `<pf-upload>`, `<pf-tooltip>`, `<FieldError>` 但均未 import
- PfForm.vue 使用 `<pf-form-item>`, `<Label>` 但未 import  
- PfDataTableForm.vue 使用 `<pf-form>`, `<pf-button>`, `<pf-text>` 但未 import
- 在 monorepo 内 Vue fallback 将它们渲染为原生 HTML 元素（input/div），docs 展示异常但侥幸不报错
- registry 单独安装时这些组件不可用

**Result**: 
- 补全 10 个组件 import
- pf-form registry 新增 registryDependencies: pf-icon-picker, pf-text, pf-tooltip, ui-primitives
- pf-data-table registry 新增 registryDependencies: pf-button, pf-text
- `pnpm build` 通过 (4142 modules, 0 errors)
- .ai/state/ known-issues, handoff, progress, current-task 同步更新

## 2026-05-22: KI-016 PfDataTable 按钮修复与 PfDatePicker 抽离

**What**: 修复 PfDataTable.vue 主组件缺失显式子组件 import 的问题，并将 PfForm 内部日期/时间/日期时间选择器抽离为 `pf-date-picker` 独立组件。

**Why**:
- 上次只修了 PfDataTableForm.vue，PfDataTable.vue 本身仍在使用未 import 的 `<pf-button>`、`<pf-card>`、`<pf-loading>`、`<pf-empty>`、`<pf-form>`，registry/docs 场景会导致操作按钮样式和行为异常。
- potato-template 中日期时间选择器原本藏在 PfForm 内部，适合抽为可单独安装和复用的输入组件。

**Result**:
- PfDataTable.vue 改为显式 import 并使用 PfButton/PfCard/PfEmpty/PfForm/PfLoading/PfDataTable* 组件。
- 新增 `src/components/pf-date-picker/`、`registry/items/pf-date-picker.json`，并导出 PfDatePicker 类型。
- PfFormItemDatetime 保留为兼容包装，PfFormItem 支持 datetime/date/time 三种类型和 range/disabled 透传。
- registry build 产出 27 items；`vue-tsc -b`、生产构建通过。

## 2026-05-22: PfCode 文档代码块与 DataTable 预览高度

**What**: 新增 `pf-code` 组件，并将 docs 页面里的安装、用法、依赖代码块从 `<pre class="code">` 统一替换为 `<PfCode>`；同时调高 PfDataTable 文档预览区域高度。

**Why**:
- 文档代码展示应复用组件能力，而不是每页维护散落的 pre 样式。
- PfDataTable 示例区域高度不足，表格和操作区在预览里看不全。

**Result**:
- 新增 `src/components/pf-code/`、`registry/items/pf-code.json`，并在 docs app 中全局注册 PfCode。
- docs 页面代码块统一使用 `<PfCode>`，保留 Form demo 的提交结果普通 `<pre>`。
- PfDataTablePage 的 preview min-height 提升到 620px，表格主体设置最小高度。
- registry build 产出 28 items；`vue-tsc -b`、生产构建通过。

## 2026-05-22: PfCode 接入 Shiki 高亮

**What**: 将 `potato-template` 中 `DocCodeBlock + src/lib/shiki.ts` 的 Shiki 高亮能力收进 PfCode 组件本身。

**Why**:
- 之前的 PfCode 只是轻量 pre/copy 包装，未承接 template 中已经验证过的 Shiki 代码高亮体验。
- PfCode 作为可分发组件，应封装高亮、主题切换和 fallback，而不是要求消费方额外理解 docs 内部实现。

**Result**:
- PfCode 内部使用 `shiki` 创建 highlighter，支持 vue/ts/js/json/html/css/scss/sql/bash/md 等语言。
- 主题跟随 `html.dark`，在 `catppuccin-latte` 与 `catppuccin-macchiato` 间切换。
- `package.json` 与 `registry/items/pf-code.json` 增加 `shiki` 依赖。
- `vue-tsc -b`、生产构建通过；registry public payload 已用 `node --experimental-strip-types scripts/build-registry.ts` 刷新。

## 2026-05-26 — AI Memory Starter 模板抽离

- What: 新增 `ai-memory-starter/`，包含通用 `.ai` 模板、行为校正、跨 Agent adapter 和初始化脚本。
- Why: 将 Potato UI 中验证过的 `.ai` 概念抽成独立启动核心，支持多工具、多模型一致开发和跨平台接续。
- Result: Starter 已去除项目事实，仅保留通用协议与占位模板；Potato UI 的 `.ai/vendor/behavior.md` 同步吸收行为护栏。

## 2026-05-28: Bug fixes, docs classification, DataTable request boundary, installer

**What**: Fixed docs/runtime bugs, reorganized docs navigation, improved interactive previews, refactored PfDataTable async boundary, and added a lightweight registry installer.

**Why**:
- shadcn-vue CLI assumptions around Tailwind config are too heavy for Potato UI's Vue 3 + UnoCSS source-distribution model.
- PfDataTable should not force consumers to install or adopt @tanstack/vue-query.
- Docs needed clearer component categories, better preview sizing, and props toggles for components such as PfColorPicker and PfImg.

**Result**:
- Checkpoint commits: bed4ac3, d9d6229, 05623fb, d02949c.
- PfToast docs now mounts PfToastProvider; PfImg preview uses viewerjs directly; PfButton disabled state shows not-allowed cursor.
- Docs nav is categorized; PfForm and PfDataTable are under complex components; ColorPicker/Img previews have props playgrounds.
- PfDataTable now uses Promise request props and no longer depends on @tanstack/vue-query; legacy listQuery/create/update/delete/detail props remain compatibility aliases.
- Added scripts/install-registry-item.ts and `pnpm pf:add <name> --cwd <target>`; pf-button dry-run and temp-dir install smoke passed.
- Validation: pnpm typecheck, pnpm registry:build, pnpm build passed. Browser plugin disconnected during DataTable interaction check, so full browser interaction should be retried later.

## 2026-05-28: Docs sidebar component subtitle

**What**: 左侧侧边栏每个组件项在中文名下方增加了组件名字 subtitle（如 PfButton、PfDataTable），Foundation 项保持不变。

**Why**: 用户在长列表中只看到中文名难以快速定位对应组件；加上 Pf 前缀的英文组件名后，一目了然，且与 registry 命名一致。

**Result**:
- `src/App.vue`: `navGroups` 中每个组件 link 增加 `subtitle` 字段；模板改为双行渲染（`.nav-label` + `.nav-subtitle`）。
- `src/docs/styles.css`: `.nav-stack button` 改为 `flex-direction: column`，新增 `.nav-label` / `.nav-subtitle` 样式（subtitle 10px 半透明 muted）。
- `pnpm typecheck` 通过，Foundation 项有空 subtitle 不渲染。

## 2026-05-29: Component coverage milestone 1 data entry basics

**What**: 新增第一批高共性数据录入组件：`PfInput`、`PfTextarea`、`PfSelect`、`PfRadioGroup`，并补齐 docs 页面、导航入口、registry manifest 与 public registry payload。

**Why**: 对齐 Naive UI、Element Plus、Vben UI 等 Vue UI 框架的共同基础面，优先补足表单和管理后台最常用的输入/选择能力，同时保持 Potato UI 源码分发和 UnoCSS token 规则。

**Result**:
- 组件均位于 `src/components/pf-*`，使用 `cn()`、`cva()` 或语义 UnoCSS token，不引入新运行时包。
- docs 新增 `/components/input`、`/components/textarea`、`/components/select`、`/components/radio-group`。
- registry build 产出 32 items。
- 阶段审查：`./node_modules/.bin/vue-tsc -b`、`node --experimental-strip-types scripts/build-registry.ts`、`node node_modules/vite/bin/vite.js build` 通过；`pnpm typecheck` 在 pnpm 层报 `fetch failed`，未进入 Vue 类型检查。

## 2026-05-29: Component coverage milestone 2 display feedback navigation

**What**: 新增 `PfBadge`、`PfAlert`、`PfTabs`、`PfPagination`，补齐 docs 页面、导航入口、i18n、registry manifest 与 public registry payload。

**Why**: 继续对齐主流 Vue UI 框架的共性组件面，补足状态标识、上下文反馈、页内切换和列表分页这些后台/中台高频基础能力。

**Result**:
- 新增组件均保持 Pf 自有源码分发，不引入新业务状态库。
- docs 新增 `/components/badge`、`/components/alert`、`/components/tabs`、`/components/pagination`。
- registry build 产出 36 items。
- 阶段审查：`./node_modules/.bin/vue-tsc -b`、`node --experimental-strip-types scripts/build-registry.ts`、`node node_modules/vite/bin/vite.js build` 通过。

## 2026-05-29: Component coverage milestone 3 loading and media basics

**What**: 新增 `PfProgress`、`PfSkeleton`、`PfAvatar`，补齐 docs 页面、导航入口、i18n、registry manifest 与 public registry payload。

**Why**: 继续补齐主流 Vue UI 框架的通用展示/加载基础件，覆盖进度、加载占位和用户实体头像这类后台页面高频需求。

**Result**:
- 新增组件均使用现有 `cn()`、`cva()` 和 UnoCSS semantic tokens，不引入新运行时依赖。
- docs 新增 `/components/progress`、`/components/skeleton`、`/components/avatar`。
- registry build 产出 39 items。
- 阶段审查：`./node_modules/.bin/vue-tsc -b`、`node --experimental-strip-types scripts/build-registry.ts`、`node node_modules/vite/bin/vite.js build` 通过。

## 2026-05-29: Primitive consolidation and overlay milestone

**What**: 将新抽出的基础组件接回复杂组件：`PfFormItem` 文本输入改用 `PfInput`，options 普通单选走 `PfSelect`，多选标签走 `PfBadge`，checkbox 变体走 `PfCheckbox`；`PfDataTable` 增加可选 `PfPagination` 并将 drawer 容器切到 `PfDrawer`。新增 `PfInputNumber`、`PfDropdown`、`PfDrawer`。

**Why**: 避免 Form/DataTable 内部继续保留一套平行输入、选择、抽屉、分页实现，减少 registry 单独安装后的依赖链缺口和行为漂移。

**Result**:
- `pf-form` registryDependencies 新增 `pf-badge`、`pf-input`、`pf-input-number`、`pf-select`。
- `pf-data-table` registryDependencies 新增 `pf-drawer`、`pf-pagination`。
- docs 新增 `/components/input-number`、`/components/dropdown`、`/components/drawer`。
- registry build 产出 42 items。
- 阶段审查：`./node_modules/.bin/vue-tsc -b`、`node --experimental-strip-types scripts/build-registry.ts`、`node node_modules/vite/bin/vite.js build` 通过。

## 2026-05-29: Input composition and form field controls

**What**: 增强 `PfInput` 的 prefix/suffix 组合能力与 clearable 行为；新增 FormItem 同步规则；新增并收口 `PfCollapse`、`PfSlider`、`PfRate`。

**Why**: 输入框需要覆盖常见前后缀图标、单位文本、清空按钮场景；所有可作为表单字段的新增基础组件必须同步接入 `PfForm`，避免复杂组件内部出现平行版本或 registry 依赖遗漏。

**Result**:
- `PfInput` 支持 `prefix` / `suffix` 插槽、`prefixIcon` / `suffixIcon`、`clearable` 与 `clear` emit。
- `.ai/core/conventions.md` 新增 FormItem 同步规则。
- `PfFormItem` 新增 `slider`、`rate` type，复用 `PfSlider`、`PfRate`；`pf-form` registryDependencies 同步更新。
- docs 新增 `/components/collapse`、`/components/slider`、`/components/rate`，Form docs 补充 slider/rate 类型。
- registry build 产出 45 items。
- 阶段审查：`./node_modules/.bin/vue-tsc -b`、`node --experimental-strip-types scripts/build-registry.ts`、`node node_modules/vite/bin/vite.js build` 通过。
